import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminProductosPage() {
  return (
    <AdminPlaceholder
      title="Productos"
      description="Listado del catálogo para el panel. Aquí se verán, filtrarán y editarán SKUs cuando haya backend."
    />
  );
}
